interface:
  display_name: "Receiving Code Review"
  short_description: "Address code review feedback safely. Use when Codex receives PR comments, review threads, requested changes, inline code review feedback, or maintainer suggestions and must classify comments, decide what to fix or discuss, apply focused changes, verify each fix, and avoid unrelated refactors."
  default_prompt: "Use $df-receiving-code-review to classify review comments, apply focused fixes, and verify each resolved issue."
