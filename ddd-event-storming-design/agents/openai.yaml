interface:
  display_name: "DDD Event Storming"
  short_description: "Pure DDD modeling from actors and events"
  default_prompt: "Use $ddd-event-storming-design to analyze business requirements with event storming, identify actors, commands, events, policies, aggregates, and read models, and produce a behavior-first DDD domain design specification."

policy:
  allow_implicit_invocation: true
